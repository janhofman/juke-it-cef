import { defineMessages } from 'react-intl';

export default defineMessages({
  onFetchError: {
    id: 'PlaylistsPage.onFetchError',
    defaultMessage: 'An error occured while fetching the list of artists.',
  },
});
